import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tomopanfriendsunitedmain',
  appName: 'tomopan-friends-united-main',
  webDir: 'dist'
};

export default config;
